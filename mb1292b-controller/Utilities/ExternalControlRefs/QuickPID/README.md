# QuickPID

Upstream:

- `https://github.com/Dlloydev/QuickPID`

Why it was selected:

- multiple anti-windup modes
- timer-oriented usage model
- derivative behavior options useful on self-balancing robots

Current state:

- tracked as an external reference only
- not vendored yet because network download was blocked
