# PID-Library

Upstream:

- `https://github.com/Majid-Derhambakhsh/PID-Library`

Why it was selected:

- closer to STM32 usage than most Arduino-first PID libraries
- good reference for API shape and practical embedded integration

Current state:

- tracked as an external reference only
- not vendored yet because network download was blocked
