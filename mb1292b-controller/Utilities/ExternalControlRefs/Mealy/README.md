# Mealy

Upstream:

- `https://github.com/bytecod3/Mealy`

Why it was selected:

- STM32 self-balancing robot
- useful as a system-level reference, not as a drop-in library

Current state:

- tracked as an external reference only
- not vendored yet because network download was blocked
